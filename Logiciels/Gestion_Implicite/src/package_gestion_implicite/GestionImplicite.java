package package_gestion_implicite;

import java.sql.ResultSet;

public class GestionImplicite {
	
	/*
	public static void main(String [] args)throws Exception{
		String nom_du_joueur ; // A changer par les donnée entrées dans la fenêtre de connexion. 
		String mot_de_passe ; // A changer par les donnée entrées dans la fenêtre de connexion. 
		
		DAO dao = new DAO();
		dao.getDBConnection();
	
		
		// Affichage de la fenêtre de connection
		Connection fenetre_connection = new Connection();
		fenetre_connection.setVisible(true);
		
		
		// Récupération des informations de connexion. ( nom , mot de passe )
		nom_du_joueur = fenetre_connection.get_authentification().getNom();
		mot_de_passe = fenetre_connection.get_authentification().getMot_de_passe();
		
		// Vérification des informations de connexion avec le DAO 
		Boolean utilisateur_existe = dao.verifier_credentiels_utilisateur(nom_du_joueur, mot_de_passe);  // Retourne True si le nom de l'utilisateur et le mot de passe existent, sinon, retourne false. 
		
		// si les informations ne sont pas valides, afficher un message d'erreur dans la fenêtre de connexion et demander à l'utilisateur de réessayer. 
		
		
		
		// ----------------- > si les information sont valides , procéder aux étapes suivantes 
		// importer les informations du joueur du DAO vers le DTO 
		
		// envoyer les informations du DTO aux items de l'editeur qui en ont besoin ( joueur, coolpoints , statistiques)
		
		// afficher l'editeur.
		ParametreUtilisateur fenetre_param_utilisateur = new ParametreUtilisateur(); 
		fenetre_param_utilisateur.setVisible(true);
		
		
		// Récupérer de l'editeur dans le DTO ( implémenter le dto dans l'editeur.) 
		
		
		
		
	}
	
	
*/
}
